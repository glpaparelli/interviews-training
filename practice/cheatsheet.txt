> CHECK OVERLAPPING INTERVALS
  x1 <= y2 && y1 <= x2 

> CONVERSIONS
- int[] to List<Integer>
  Arrays.stream(array).boxed().toList();
- int[] to Integer[]
  Arrays.stream(array).boxed().toArray(Integer[]::new);
- List<Integer> to int[]
  list.stream().mapToInt(i->i).toArray();
- List<Integer> to Integer[]
  list.stream().toArray(Integer[]::new);

> REMOVE ALL NON ALPHANUMERIC CHARS FROM STRING
  s.replaceAll("[^a-zA-Z0-9]", "");

> BINARY SEARCH THAT RETURNS INDEX ELEMENT OR THE SUPPOSED INDEX ELEMENT
        int low = 0, high = nums.length-1;
        while(low <= high){
            int mid = low + (high-low)/2;
            
            if(nums[mid] == target) return mid;
            else if(nums[mid] > target) high = mid-1;
            else low = mid+1;
        }
        return low; // -1 and this is BS